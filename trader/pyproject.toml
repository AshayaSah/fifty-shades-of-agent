[project]
name = "trader"
version = "0.1.0"
description = "MCP server for Exness trading integration"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "mcp[cli]>=2.1.0",
    "python-dotenv>=1.2.3",
]
